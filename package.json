{
  "name": "overwrite-place",
  "version": "0.1.0",
  "private": true,
  "description": "One canvas. Only one artwork is alive at a time.",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test \"test/*.test.js\"",
    "build": "node scripts/build.js",
    "verify": "node scripts/verify.js",
    "preview": "node scripts/preview.js"
  },
  "devDependencies": {
    "wrangler": "^4.82.2"
  }
}
